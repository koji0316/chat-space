.side-bar
  .side-header
    .side-header__box
      %p.side-header__box__user-name
        = current_user.name
      %ul.side-header__box__icon
        %li.side-header__box__icon-l1
          = link_to new_group_path do
            = icon('far', 'edit', class: "side-header__box__icon-link1")
        %li.side-header__box__icon-l2
          = link_to edit_user_path(current_user) do
            = icon('fas', 'cog', class: "side-header__box__icon-link2")
  .side-lists
    - current_user.groups.each do |group|
      .side-list
        = link_to '#' do
          .side-list__group-name
            = group.name
        .side-list__message
          メッセージはまだありません